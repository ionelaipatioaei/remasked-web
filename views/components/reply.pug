include icon.pug
include button.pug

mixin Reply(ref)
  div.reply-container
    textarea.reply-textarea(rows="8" cols="80" placeholder="Type your reply here...")
    div.reply-textarea-buttons-container
      +Button("Reply", `commentReplyMain('${ref}')`)