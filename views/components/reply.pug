include icon.pug
include button.pug

mixin Reply(ref)
  div.reply-container
    textarea.input-textarea(placeholder="Type your reply here...")
    div.reply-textarea-buttons-container
      +Button("Reply", `commentReplyMain('${ref}')`)